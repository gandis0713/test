export interface Windowing {
  windowWidth: number;
  windowLevel: number;
}

export interface SliceOrientation {
  sliceNormal: number[];
  viewUp: number[];
}

export interface ViewApis {
  uid: string;
  type: string;
}
